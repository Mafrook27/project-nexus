import { SettingsSkeleton } from '@/components/skeletons/PageSkeletons';

/** Shown by Next.js the moment you navigate here, before the page renders. */
export default function Loading() {
  return <SettingsSkeleton />;
}
